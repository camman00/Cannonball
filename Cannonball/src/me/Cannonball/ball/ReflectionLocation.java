package me.Cannonball.ball;

public enum ReflectionLocation {
	/*
	 * ReflectionLocation for the boxes or the determining enumerator used to
	 * find if the ball should change in x or y / negatively or positively
	 * in a direction
	 */
	RIGHT,LEFT,TOP,BOTTOM;
}
